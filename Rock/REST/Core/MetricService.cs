//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by the T4\Model.tt template.
//
//     Changes to this file will be lost when the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
//
// THIS WORK IS LICENSED UNDER A CREATIVE COMMONS ATTRIBUTION-NONCOMMERCIAL-
// SHAREALIKE 3.0 UNPORTED LICENSE:
// http://creativecommons.org/licenses/by-nc-sa/3.0/
//
using System.ComponentModel.Composition;
using System.Linq;
using System.ServiceModel;
using System.ServiceModel.Activation;
using System.ServiceModel.Web;

namespace Rock.REST.Core
{
	/// <summary>
	/// REST WCF service for Metrics
	/// </summary>
    [Export(typeof(IService))]
    [ExportMetadata("RouteName", "Core/Metric")]
	[AspNetCompatibilityRequirements( RequirementsMode = AspNetCompatibilityRequirementsMode.Allowed )]
    public partial class MetricService : IMetricService, IService
    {
		/// <summary>
		/// Gets a Metric object
		/// </summary>
		[WebGet( UriTemplate = "{id}" )]
        public Rock.Core.DTO.Metric Get( string id )
        {
            var currentUser = Rock.CMS.UserService.GetCurrentUser();
            if ( currentUser == null )
                throw new WebFaultException<string>("Must be logged in", System.Net.HttpStatusCode.Forbidden );

            using (Rock.Data.UnitOfWorkScope uow = new Rock.Data.UnitOfWorkScope())
            {
				uow.objectContext.Configuration.ProxyCreationEnabled = false;
				Rock.Core.MetricService metricService = new Rock.Core.MetricService();
				Rock.Core.Metric Metric = metricService.Get( int.Parse( id ) );
				if ( Metric.Authorized( "View", currentUser ) )
					return Metric.DataTransferObject;
				else
					throw new WebFaultException<string>( "Not Authorized to View this Metric", System.Net.HttpStatusCode.Forbidden );
            }
        }
		
		/// <summary>
		/// Gets a Metric object
		/// </summary>
		[WebGet( UriTemplate = "{id}/{apiKey}" )]
        public Rock.Core.DTO.Metric ApiGet( string id, string apiKey )
        {
            using (Rock.Data.UnitOfWorkScope uow = new Rock.Data.UnitOfWorkScope())
            {
				Rock.CMS.UserService userService = new Rock.CMS.UserService();
                Rock.CMS.User user = userService.Queryable().Where( u => u.ApiKey == apiKey ).FirstOrDefault();

				if (user != null)
				{
					uow.objectContext.Configuration.ProxyCreationEnabled = false;
					Rock.Core.MetricService metricService = new Rock.Core.MetricService();
					Rock.Core.Metric Metric = metricService.Get( int.Parse( id ) );
					if ( Metric.Authorized( "View", user ) )
						return Metric.DataTransferObject;
					else
						throw new WebFaultException<string>( "Not Authorized to View this Metric", System.Net.HttpStatusCode.Forbidden );
				}
				else
					throw new WebFaultException<string>( "Invalid API Key", System.Net.HttpStatusCode.Forbidden );
            }
        }
		
		/// <summary>
		/// Updates a Metric object
		/// </summary>
		[WebInvoke( Method = "PUT", UriTemplate = "{id}" )]
        public void UpdateMetric( string id, Rock.Core.DTO.Metric Metric )
        {
            var currentUser = Rock.CMS.UserService.GetCurrentUser();
            if ( currentUser == null )
                throw new WebFaultException<string>("Must be logged in", System.Net.HttpStatusCode.Forbidden );

            using ( Rock.Data.UnitOfWorkScope uow = new Rock.Data.UnitOfWorkScope() )
            {
				uow.objectContext.Configuration.ProxyCreationEnabled = false;
				Rock.Core.MetricService metricService = new Rock.Core.MetricService();
				Rock.Core.Metric existingMetric = metricService.Get( int.Parse( id ) );
				if ( existingMetric.Authorized( "Edit", currentUser ) )
				{
					uow.objectContext.Entry(existingMetric).CurrentValues.SetValues(Metric);
					
					if (existingMetric.IsValid)
						metricService.Save( existingMetric, currentUser.PersonId );
					else
						throw new WebFaultException<string>( existingMetric.ValidationResults.AsDelimited(", "), System.Net.HttpStatusCode.BadRequest );
				}
				else
					throw new WebFaultException<string>( "Not Authorized to Edit this Metric", System.Net.HttpStatusCode.Forbidden );
            }
        }

		/// <summary>
		/// Updates a Metric object
		/// </summary>
		[WebInvoke( Method = "PUT", UriTemplate = "{id}/{apiKey}" )]
        public void ApiUpdateMetric( string id, string apiKey, Rock.Core.DTO.Metric Metric )
        {
            using ( Rock.Data.UnitOfWorkScope uow = new Rock.Data.UnitOfWorkScope() )
            {
				Rock.CMS.UserService userService = new Rock.CMS.UserService();
                Rock.CMS.User user = userService.Queryable().Where( u => u.ApiKey == apiKey ).FirstOrDefault();

				if (user != null)
				{
					uow.objectContext.Configuration.ProxyCreationEnabled = false;
					Rock.Core.MetricService metricService = new Rock.Core.MetricService();
					Rock.Core.Metric existingMetric = metricService.Get( int.Parse( id ) );
					if ( existingMetric.Authorized( "Edit", user ) )
					{
						uow.objectContext.Entry(existingMetric).CurrentValues.SetValues(Metric);
					
						if (existingMetric.IsValid)
							metricService.Save( existingMetric, user.PersonId );
						else
							throw new WebFaultException<string>( existingMetric.ValidationResults.AsDelimited(", "), System.Net.HttpStatusCode.BadRequest );
					}
					else
						throw new WebFaultException<string>( "Not Authorized to Edit this Metric", System.Net.HttpStatusCode.Forbidden );
				}
				else
					throw new WebFaultException<string>( "Invalid API Key", System.Net.HttpStatusCode.Forbidden );
            }
        }

		/// <summary>
		/// Creates a new Metric object
		/// </summary>
		[WebInvoke( Method = "POST", UriTemplate = "" )]
        public void CreateMetric( Rock.Core.DTO.Metric Metric )
        {
            var currentUser = Rock.CMS.UserService.GetCurrentUser();
            if ( currentUser == null )
                throw new WebFaultException<string>("Must be logged in", System.Net.HttpStatusCode.Forbidden );

            using ( Rock.Data.UnitOfWorkScope uow = new Rock.Data.UnitOfWorkScope() )
            {
				uow.objectContext.Configuration.ProxyCreationEnabled = false;
				Rock.Core.MetricService metricService = new Rock.Core.MetricService();
				Rock.Core.Metric existingMetric = new Rock.Core.Metric();
				metricService.Add( existingMetric, currentUser.PersonId );
				uow.objectContext.Entry(existingMetric).CurrentValues.SetValues(Metric);

				if (existingMetric.IsValid)
					metricService.Save( existingMetric, currentUser.PersonId );
				else
					throw new WebFaultException<string>( existingMetric.ValidationResults.AsDelimited(", "), System.Net.HttpStatusCode.BadRequest );
            }
        }

		/// <summary>
		/// Creates a new Metric object
		/// </summary>
		[WebInvoke( Method = "POST", UriTemplate = "{apiKey}" )]
        public void ApiCreateMetric( string apiKey, Rock.Core.DTO.Metric Metric )
        {
            using ( Rock.Data.UnitOfWorkScope uow = new Rock.Data.UnitOfWorkScope() )
            {
				Rock.CMS.UserService userService = new Rock.CMS.UserService();
                Rock.CMS.User user = userService.Queryable().Where( u => u.ApiKey == apiKey ).FirstOrDefault();

				if (user != null)
				{
					uow.objectContext.Configuration.ProxyCreationEnabled = false;
					Rock.Core.MetricService metricService = new Rock.Core.MetricService();
					Rock.Core.Metric existingMetric = new Rock.Core.Metric();
					metricService.Add( existingMetric, user.PersonId );
					uow.objectContext.Entry(existingMetric).CurrentValues.SetValues(Metric);

					if (existingMetric.IsValid)
						metricService.Save( existingMetric, user.PersonId );
					else
						throw new WebFaultException<string>( existingMetric.ValidationResults.AsDelimited(", "), System.Net.HttpStatusCode.BadRequest );
				}
				else
					throw new WebFaultException<string>( "Invalid API Key", System.Net.HttpStatusCode.Forbidden );
            }
        }

		/// <summary>
		/// Deletes a Metric object
		/// </summary>
		[WebInvoke( Method = "DELETE", UriTemplate = "{id}" )]
        public void DeleteMetric( string id )
        {
            var currentUser = Rock.CMS.UserService.GetCurrentUser();
            if ( currentUser == null )
                throw new WebFaultException<string>("Must be logged in", System.Net.HttpStatusCode.Forbidden );

            using ( Rock.Data.UnitOfWorkScope uow = new Rock.Data.UnitOfWorkScope() )
            {
				uow.objectContext.Configuration.ProxyCreationEnabled = false;
				Rock.Core.MetricService metricService = new Rock.Core.MetricService();
				Rock.Core.Metric Metric = metricService.Get( int.Parse( id ) );
				if ( Metric.Authorized( "Edit", currentUser ) )
				{
					metricService.Delete( Metric, currentUser.PersonId );
					metricService.Save( Metric, currentUser.PersonId );
				}
				else
					throw new WebFaultException<string>( "Not Authorized to Edit this Metric", System.Net.HttpStatusCode.Forbidden );
            }
        }

		/// <summary>
		/// Deletes a Metric object
		/// </summary>
		[WebInvoke( Method = "DELETE", UriTemplate = "{id}/{apiKey}" )]
        public void ApiDeleteMetric( string id, string apiKey )
        {
            using ( Rock.Data.UnitOfWorkScope uow = new Rock.Data.UnitOfWorkScope() )
            {
				Rock.CMS.UserService userService = new Rock.CMS.UserService();
                Rock.CMS.User user = userService.Queryable().Where( u => u.ApiKey == apiKey ).FirstOrDefault();

				if (user != null)
				{
					uow.objectContext.Configuration.ProxyCreationEnabled = false;
					Rock.Core.MetricService metricService = new Rock.Core.MetricService();
					Rock.Core.Metric Metric = metricService.Get( int.Parse( id ) );
					if ( Metric.Authorized( "Edit", user ) )
					{
						metricService.Delete( Metric, user.PersonId );
						metricService.Save( Metric, user.PersonId );
					}
					else
						throw new WebFaultException<string>( "Not Authorized to Edit this Metric", System.Net.HttpStatusCode.Forbidden );
				}
				else
					throw new WebFaultException<string>( "Invalid API Key", System.Net.HttpStatusCode.Forbidden );
            }
        }

    }
}
