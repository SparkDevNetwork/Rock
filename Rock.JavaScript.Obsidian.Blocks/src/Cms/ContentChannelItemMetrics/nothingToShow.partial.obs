<!-- Copyright by the Spark Development Network; Licensed under the Rock Community License -->
<template>
    <div class="empty-state content-channel-item-metrics-empty-state">
        <i class="empty-state-icon ti ti-alert-triangle"></i>
        <div class="empty-state-body">
            <p class="empty-state-text">{{ text }}</p>
            <small class="empty-state-help-text">{{ helpText }}</small>
        </div>
    </div>
</template>

<script setup lang="ts">
    withDefaults(defineProps<{
        /** The primary empty-state message. */
        text?: string;

        /** The secondary helper line below the message. */
        helpText?: string;
    }>(), {
        text: "Nothing To Show",
        helpText: "Not enough data has been collected yet. Check again later."
    });
</script>
