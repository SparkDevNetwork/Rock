<!-- Copyright by the Spark Development Network; Licensed under the Rock Community License -->
<template>
    <div class="nothing-to-show-container">
        <i class="nothing-to-show-icon ti ti-alert-triangle"></i>
        <div class="nothing-to-show-body">
            <p class="nothing-to-show-text">{{ text }}</p>
            <small class="nothing-to-show-help-text">{{ helpText }}</small>
        </div>
    </div>
</template>

<style scoped>
.nothing-to-show-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-small);
    min-height: 300px;
    color: var(--color-interface-medium);
}

.nothing-to-show-icon {
    font-size: 60px;
    line-height: 1;
}

.nothing-to-show-body {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-tiny);
}

.nothing-to-show-text {
    margin: 0;
}

.nothing-to-show-help-text {
    margin: 0;
    font-size: var(--font-size-xsmall);
}
</style>

<script setup lang="ts">
    withDefaults(defineProps<{
        /** The primary empty-state message. */
        text?: string;

        /** The secondary helper line below the message. */
        helpText?: string;
    }>(), {
        text: "Nothing To Show",
        helpText: "Not enough data has been collected yet. Check again later."
    });
</script>
