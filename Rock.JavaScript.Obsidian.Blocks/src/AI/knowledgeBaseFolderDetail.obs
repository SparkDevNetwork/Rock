<!-- Copyright by the Spark Development Network; Licensed under the Rock Community License -->
<template>
    <NotificationBox v-if="blockError" alertType="warning" v-html="blockError"></NotificationBox>

    <NotificationBox v-if="errorMessage" alertType="danger" v-html="errorMessage"></NotificationBox>

    <DetailBlock v-if="!blockError"
                 v-model:mode="panelMode"
                 :name="panelName"
                 :labels="blockLabels"
                 :entityKey="entityKey"
                 :isAuditHidden="false"
                 :isBadgesVisible="false"
                 :isDeleteVisible="isEditable"
                 :isEditVisible="isEditable"
                 :isFollowVisible="false"
                 :isSecurityHidden="true"
                 @cancelEdit="onCancelEdit"
                 @delete="onDelete"
                 @edit="onEdit"
                 @save="onSave">
        <template #view>
            <ViewPanel :modelValue="folderViewBag" :options="options" />
        </template>

        <template #edit>
            <EditPanel v-model="folderEditBag" :options="options" />
        </template>
    </DetailBlock>
</template>

<script setup lang="ts">
    import { computed, ref } from "vue";
    import NotificationBox from "@Obsidian/Controls/notificationBox.obs";
    import DetailBlock from "@Obsidian/Templates/detailBlock";
    import { DetailPanelMode } from "@Obsidian/Enums/Controls/detailPanelMode";
    import { PanelAction } from "@Obsidian/Types/Controls/panelAction";
    import EditPanel from "./KnowledgeBaseFolderDetail/editPanel.partial.obs";
    import ViewPanel from "./KnowledgeBaseFolderDetail/viewPanel.partial.obs";
    import { onConfigurationValuesChanged, useConfigurationValues, useEntityDetailBlock, useInvokeBlockAction, useReloadBlock } from "@Obsidian/Utility/block";
    import { NavigationUrlKey } from "./KnowledgeBaseFolderDetail/types.partial";
    import { DetailBlockBox } from "@Obsidian/ViewModels/Blocks/detailBlockBox";
    import { KnowledgeBaseFolderBag } from "@Obsidian/ViewModels/Blocks/AI/KnowledgeBaseFolderDetail/knowledgeBaseFolderBag";
    import { KnowledgeBaseFolderDetailOptionsBag } from "@Obsidian/ViewModels/Blocks/AI/KnowledgeBaseFolderDetail/knowledgeBaseFolderDetailOptionsBag";
    import { ValidPropertiesBox } from "@Obsidian/ViewModels/Utility/validPropertiesBox";

    const config = useConfigurationValues<DetailBlockBox<KnowledgeBaseFolderBag, KnowledgeBaseFolderDetailOptionsBag>>();
    const invokeBlockAction = useInvokeBlockAction();

    onConfigurationValuesChanged(useReloadBlock());

    // #region Values

    const blockError = ref("");
    const errorMessage = ref("");

    const folderViewBag = ref(config.entity);
    const folderEditBag = ref<ValidPropertiesBox<KnowledgeBaseFolderBag>>({
        bag: {} as KnowledgeBaseFolderBag
    });

    const panelMode = ref<DetailPanelMode>(DetailPanelMode.View);

    useEntityDetailBlock({
        blockConfig: config,
        entity: folderEditBag
    });

    // #endregion

    // #region Computed Values

    const panelName = computed((): string => {
        return folderViewBag.value?.name ?? "";
    });

    const entityKey = computed((): string => {
        return folderViewBag.value?.idKey ?? "";
    });

    const blockLabels = computed((): PanelAction[] | undefined => {
        if (panelMode.value !== DetailPanelMode.View) {
            return undefined;
        }

        return [];
    });

    const isEditable = computed((): boolean => {
        return config.isEditable === true;
    });

    const options = computed((): KnowledgeBaseFolderDetailOptionsBag => {
        return config.options ?? {};
    });

    // #endregion

    // #region Event Handlers

    async function onCancelEdit(): Promise<boolean | string> {
        if (!folderEditBag.value.bag?.idKey) {
            if (config.navigationUrls?.[NavigationUrlKey.ParentPage]) {
                return config.navigationUrls[NavigationUrlKey.ParentPage];
            }

            return false;
        }

        return true;
    }

    async function onDelete(): Promise<false | string> {
        errorMessage.value = "";

        const result = await invokeBlockAction<string>("Delete", {
            key: folderViewBag.value?.idKey
        });

        if (result.isSuccess && result.data) {
            return result.data;
        }
        else {
            errorMessage.value = result.errorMessage ?? "Unknown error while trying to delete folder.";

            return false;
        }
    }

    async function onEdit(): Promise<boolean> {
        const result = await invokeBlockAction<ValidPropertiesBox<KnowledgeBaseFolderBag>>("Edit", {
            key: folderViewBag.value?.idKey
        });

        if (result.isSuccess && result.data && result.data.bag) {
            folderEditBag.value = {
                bag: result.data.bag,
                validProperties: result.data.validProperties ?? Object.keys(result.data.bag)
            };

            return true;
        }
        else {
            return false;
        }
    }

    async function onSave(): Promise<boolean | string> {
        errorMessage.value = "";

        const result = await invokeBlockAction<ValidPropertiesBox<KnowledgeBaseFolderBag> | string>("Save", {
            box: folderEditBag.value
        });

        if (result.isSuccess && result.data) {
            if (result.statusCode === 200 && typeof result.data === "object") {
                folderViewBag.value = result.data.bag;

                return true;
            }
            else if (result.statusCode === 201 && typeof result.data === "string") {
                return result.data;
            }
        }

        errorMessage.value = result.errorMessage ?? "Unknown error while trying to save folder.";

        return false;
    }

    // #endregion

    // Handle any initial error conditions or the need to go into edit mode.
    if (config.errorMessage) {
        blockError.value = config.errorMessage;
    }
    else if (!config.entity) {
        blockError.value = "The specified knowledge base folder could not be viewed.";
    }
    else if (!config.entity.idKey) {
        folderEditBag.value = { bag: config.entity };
        panelMode.value = DetailPanelMode.Add;
    }
    else if (isEditable.value && new URLSearchParams(window.location.search).get("autoEdit")?.toLowerCase() === "true") {
        // The folder list linked here with autoEdit=true; jump straight into
        // the edit form. View and edit bags share the same shape so we can
        // seed the edit bag from config.entity without an extra block action.
        folderEditBag.value = { bag: config.entity };
        panelMode.value = DetailPanelMode.Edit;
    }
</script>
