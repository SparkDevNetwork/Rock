<!-- Copyright by the Spark Development Network; Licensed under the Rock Community License -->
<template>
    <Modal v-model="isShown" title="Move Group Member" saveText="Move">
        <!-- TODO: Current Group static text, Destination Group picker (required), destination Role
             dropdown from GetMoveGroupMemberOptions, Move Notes checkbox, Move Fundraising
             Transactions checkbox (fundraising group types only), and warning alerts per conversion
             plan §9.8. -->
    </Modal>
</template>

<script setup lang="ts">
    import { PropType } from "vue";
    import Modal from "@Obsidian/Controls/modal.obs";
    import { useVModelPassthrough } from "@Obsidian/Utility/component";

    const props = defineProps({
        modelValue: {
            type: Boolean as PropType<boolean>,
            required: true
        }
    });

    const emit = defineEmits<{
        (e: "update:modelValue", value: boolean): void;
    }>();

    // #region Values

    const isShown = useVModelPassthrough(props, "modelValue", emit);

    // #endregion Values
</script>
