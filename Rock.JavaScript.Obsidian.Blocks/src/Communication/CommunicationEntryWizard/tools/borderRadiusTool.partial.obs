<template>
    <NumberUpDown v-model="borderRadius"
                  :label="label"
                  :min="min"
                  :max="max" />
</template>

<script setup lang="ts">
    import { PropType, ref, watch } from "vue";
    import NumberUpDown from "@Obsidian/Controls/numberUpDown.obs";

    const props = defineProps({
        element: {
            type: Object as PropType<HTMLElement>,
            required: true
        },

        label: {
            type: String as PropType<string>,
            default: "Border Radius" as const
        },

        min: {
            type: Number as PropType<number>,
            default: 0
        },

        max: {
            type: Number as PropType<number>,
            default: 700
        }
    });

    const emit = defineEmits<{
        (e: "changeShorthand", value: string): void;
    }>();

    // #region Values

    const borderRadius = ref<number>(parseInt(props.element.style.borderRadius || "0"));

    // #endregion

    // #region Watchers

    watch(borderRadius, (value: number) => {
        const borderRadiusValue = value ? `${value}px` : "";
        props.element.style.borderRadius = borderRadiusValue;

        emit("changeShorthand", borderRadiusValue);
    });

    watch(() => props.element, () => {
        borderRadius.value = parseInt(props.element.style.borderRadius || "0");
    });

    // #endregion
</script>