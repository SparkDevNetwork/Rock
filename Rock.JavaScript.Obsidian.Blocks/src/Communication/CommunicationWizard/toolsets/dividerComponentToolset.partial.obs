<template>
    <ComponentToolset @complete="$emit('complete')"
                      @delete="$emit('delete')">
        <AccordionGroup>
            <Accordion title="General Settings">
                <template v-if="hrElement">
                    <BorderColorTool :modelValue="hrElement" label="Color" :clearBackgroundColor="true" />
                    <BorderStyleTool :modelValue="hrElement" />
                    <BorderWidthTool :modelValue="hrElement" />
                    <MarginTool :modelValue="hrElement" />
                </template>
            </Accordion>
        </AccordionGroup>
    </ComponentToolset>
</template>

<script setup lang="ts">
    import { computed, PropType } from "vue";
    import ComponentToolset from "./componentToolset.partial.obs";
    import Accordion from "../accordion.partial.obs";
    import AccordionGroup from "../accordionGroup.partial.obs";
    import BorderColorTool from "../tools/borderColorTool.partial.obs";
    import BorderStyleTool from "../tools/borderStyleTool.partial.obs";
    import BorderWidthTool from "../tools/borderWidthTool.partial.obs";
    import MarginTool from "../tools/marginTool.partial.obs";
    import { EditorComponent } from "../types.partial";

    const props = defineProps({
        modelValue: {
            type: Object as PropType<EditorComponent>,
            required: true
        }
    });

    defineEmits<{
        (e: "complete"): void;
        (e: "delete"): void;
    }>();

    // #region Computed Values

    const hrElement = computed<HTMLHRElement | null | undefined>(() => {
        return props.modelValue.componentElement?.querySelector("hr");
    });

    // #endregion
</script>