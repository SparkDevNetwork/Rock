<template>
    <NumberUpDown :modelValue="modelValue"
                  @update:modelValue="onUpdateModelValue"
                  label="Border Size"
                  :min="0" />
</template>

<script setup lang="ts">
    import { PropType, ref, watch } from "vue";
    import { NumberUpDown } from "../imports.partial";

    const props = defineProps({
        modelValue: {
            type: Object as PropType<HTMLElement>,
            required: true
        }
    });

    // #region Values

    const modelValue = ref<number>(parseInt(props.modelValue.style.borderWidth || "0"));

    // #endregion

    // #region Event Handlers

    function onUpdateModelValue(value: number): void {
        setValue(value);
    }

    // #endregion

    // #region Functions

    function setValue(value: number): void {
        modelValue.value = value;

        if (value) {
            props.modelValue.style.borderWidth = `${value}px`;

            if (!props.modelValue.style.borderStyle) {
                // Ensure the element has a border style if a border color is specified.
                props.modelValue.style.borderStyle = "solid";
            }
        }
        else {
            props.modelValue.style.borderWidth = "";
            props.modelValue.style.borderStyle = "";
        }
    }

    // #endregion

    // #region Watchers

    watch(() => props.modelValue, () => {
        setValue(parseInt(props.modelValue.style.borderWidth || "0"));
    });

    // #endregion
</script>