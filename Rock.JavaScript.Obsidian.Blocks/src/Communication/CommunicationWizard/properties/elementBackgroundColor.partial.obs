<template>
    <ColorPicker v-model="modelValue"
                 :label="label" />
</template>

<script setup lang="ts">
    import { PropType, ref, watch } from "vue";
    import { ColorPicker } from "../imports.partial";

    const props = defineProps({
        modelValue: {
            type: Object as PropType<HTMLElement>,
            required: true
        },

        label: {
            type: String as PropType<string>,
            default: "Background Color"
        }
    });

    // #region Values

    const modelValue = ref<string>(props.modelValue.style.backgroundColor ?? "");

    // #endregion

    // #region Watchers

    watch(modelValue, (value: string) => {
        props.modelValue.style.backgroundColor = value;
    });

    watch(() => props.modelValue, () => {
        modelValue.value = props.modelValue.style.backgroundColor;
    });

    // #endregion
</script>