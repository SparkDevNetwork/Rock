# Enforce the engines floor in package.json so installs on an npm older than
# 11.5.0 fail fast instead of silently re-adding pruned optional peer deps
# (e.g. ts-jest's jest 30.x optional peers) to package-lock.json.
engine-strict=true
