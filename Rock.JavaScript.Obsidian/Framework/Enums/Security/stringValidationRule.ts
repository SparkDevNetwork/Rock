//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by the Rock.CodeGeneration project
//     Changes to this file will be lost when the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
// <copyright>
// Copyright by the Spark Development Network
//
// Licensed under the Rock Community License (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.rockrms.com/license
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
// </copyright>
//

/**
 * Individual content-policy rules that can be evaluated against a string value
 * at the persistence boundary. Each member is a single rule that fires when its
 * pattern is detected, or for the one allowlist rule, when the value contains
 * any character outside the allowed set. Profiles compose these flags into named
 * bundles; see Rock.Enums.Security.StringValidationProfile.
 */
export const StringValidationRule = {
    /**
     * No rule selected. Used as the empty bitmask; an attribute whose effective
     * rule set is Rock.Enums.Security.StringValidationRule.None performs no validation against the value.
     */
    None: 0x0000,

    /**
     * Detects Lava output expressions ( {{ ... }} ) that render a merge-field
     * value. Lower risk than Rock.Enums.Security.StringValidationRule.LavaCommands because output expressions
     * cannot invoke side effects, but still flagged as injection when the property
     * is not meant to be Lava-evaluated.
     */
    LavaFormatting: 0x0001,

    /**
     * Detects Lava tags ( {% ... %} ) and Lava shortcodes ( {[ ... ]} ).
     * These delimiters can invoke entity commands, web requests, file operations,
     * and other side effects, and are higher risk than Rock.Enums.Security.StringValidationRule.LavaFormatting.
     */
    LavaCommands: 0x0002,

    /**
     * Detects the literal sequence &lt;script followed by a word boundary,
     * case-insensitive. Matches a browser-parseable script tag opening.
     */
    ScriptTags: 0x0004,

    /**
     * Detects the javascript: URL scheme when preceded by an HTML-attribute
     * or CSS-url context character ( =, ", ', or ( ),
     * so prose mentioning "javascript:" is not flagged. Does not match
     * entity-encoded forms; URL-typed fields should layer a separate scheme-allowlist
     * validation on top to catch bare values.
     */
    JavascriptProtocol: 0x0008,

    /**
     * Detects HTML event-handler attribute names (e.g. onclick=,
     * onerror=) drawn from the HTML Living Standard's enumerated list,
     * with the common on prefix factored out for performance. Uses an
     * explicit name list rather than a generic on[a-z]+ pattern to avoid
     * false positives on prose like "online =".
     */
    EventHandlerAttributes: 0x0010,

    /**
     * Detects any HTML tag start: &lt; immediately followed by an ASCII
     * letter, !, or /. Matches what HTML5 browsers tokenize as a
     * tag, so math/inequality usage like A &lt; B (with whitespace) is
     * intentionally allowed and not flagged.
     */
    AnyHtmlTags: 0x0020,

    /**
     * Detects ASCII control characters: \x00–\x08, \x0B,
     * \x0C, \x0E–\x1F, and \x7F (DEL). Tab, line-feed,
     * and carriage-return are explicitly allowed so multi-line text fields work.
     * The null byte is included in this range; there is no separate null-byte rule.
     */
    ControlCharacters: 0x0040,

    /**
     * Detects Unicode bidirectional-override characters ( U+202A–U+202E,
     * U+2066–U+2069 ) used in bidi-spoofing attacks. Real
     * right-to-left script characters (Arabic, Hebrew, etc.) are different code
     * points and trigger directionality without these override characters, so this
     * rule does not interfere with international text.
     */
    BidiOverrides: 0x0080
} as const;

/**
 * Individual content-policy rules that can be evaluated against a string value
 * at the persistence boundary. Each member is a single rule that fires when its
 * pattern is detected, or for the one allowlist rule, when the value contains
 * any character outside the allowed set. Profiles compose these flags into named
 * bundles; see Rock.Enums.Security.StringValidationProfile.
 */
export const StringValidationRuleDescription: Record<number, string> = {
    0: "None",

    1: "Lava Formatting",

    2: "Lava Commands",

    4: "Script Tags",

    8: "Javascript Protocol",

    16: "Event Handler Attributes",

    32: "Any Html Tags",

    64: "Control Characters",

    128: "Bidi Overrides"
};

/**
 * Individual content-policy rules that can be evaluated against a string value
 * at the persistence boundary. Each member is a single rule that fires when its
 * pattern is detected, or for the one allowlist rule, when the value contains
 * any character outside the allowed set. Profiles compose these flags into named
 * bundles; see Rock.Enums.Security.StringValidationProfile.
 */
export type StringValidationRule = number;
