# Completed Specs Index

This index lists every spec that has been moved into `specs/completed/`. It is maintained by the `spec` skill, please do not edit by hand.

| Spec | Domain | Author | Summary | Commit |
|------|--------|--------|---------|--------|
| [Lava → Fluid Bridge: Performance and Allocation Improvements](lava/260501-lava-fluid-bridge-perf-improvements.md) | Lava | Jon Edmiston | Performance and allocation review of the Lava → Fluid bridge introduced when Rock moved from DotLiquid to Fluid. Catalogs hot-path reflection, redundant per-render parsing, async-over-sync allocations, and a few latent thread safety bugs, with a checkbox per finding so reviewers can pick what to act on. | `8525c2c2da3cab13b113e79866cefb09dab42f74` |
| [Lava Engine Abstraction: Performance and Allocation Improvements](lava/260501-lava-engine-abstraction-perf-improvements.md) | Lava | Jon Edmiston | Performance and allocation review of the engine-agnostic Lava abstraction that sits between Rock and the active Lava engine (LavaService, LavaHelper, LavaEngineBase, WebsiteLavaTemplateCacheService, ResolveMergeFields, and related plumbing). Catalogs reflection-per-call hotspots, redundant pre-flight checks, per-render allocation, mutable-input bugs, and a few thread-safety issues, each as a checkbox so reviewers can pick what to act on. | `19d5369f8984e4fe71daf8fddae5c67cd2bdd290` |
| [Rock User-Agent Helper](core/260506-rock-user-agent-helper.md) | Core | Daniel Hazelbaker | Rock parses user-agent strings ad hoc at multiple call sites with no shared caching. UAParser's Parse runs ~15,000 regex evaluations per invocation, so any hot-path consumer pays heavily and there is no shared result cache. This spec defines a single Rock-owned helper plus a fresh Rock-owned POCO (designed from observed call-site needs, not as a UAParser mirror) so that UAParser becomes an internal implementation detail of one helper. The existing partial cache in Rock.Net.ClientInformation is the seed for the new helper. The existing Rock.Web.BrowserInfo / BrowserOS / BrowserDevice / BrowserUserAgent types are not reused (only two in-repo callers, both migratable) and the entire BrowserClient family is marked obsolete as part of this work. Rock.Model.InteractionDeviceType.GetClientType is also obsoleted; its regex logic is absorbed into the new POCO's ClientType property so the result piggybacks on the user-agent-string cache. | `28a9cffe5d2c22f27927839e63e6fe4b28eb3dc0` |
| [Inbound Shortlink URL UTM Fallback](cms/260507-inbound-shortlink-url-utm-fallback.md) | CMS | Jon Edmiston | When a visitor follows a shortlink with utm_* query parameters appended to the inbound URL, those values are dropped today because the route handler reads UTMs only from the resolved destination URL. Honor the inbound UTMs as a per-key fallback so a configured shortlink value still wins, but inbound values fill in for any UTM key the shortlink does not set. | `600919e68caa25961c6468eb9227ad321eb2081a` |
| [Server-Side Field Validation](core/260512-server-side-field-validation.md) | Core | Daniel Hazelbaker | Add an additional server-side input validation layer that complements Rock's existing protections and provides consistent, defense-in-depth enforcement at the data-ingestion boundary. Validation is driven by named profiles (Unrestricted, BasicHtml, LavaAndBasicHtml, PlainText, Name) declared via a property attribute, with PlainText as the secure default for any undecorated property. | `57a82f25769d247eb50a36a6901190254693fdc0` |
| [Check-in Areas and Groups: WebForms to Obsidian Conversion](check-in/260506-check-in-areas-and-groups-obsidian-conversion.md) | Check-in | Jason Hendee | Convert the legacy WebForms CheckinAreas block to a new Obsidian CheckInAreasAndGroups block, targeting the Figma redesign rather than legacy parity. Foundation, generic SortableTree control, sibling-only / same-type-only drag-and-drop reorder with server persistence, the Area editor (conditional attributes well plus Check-in / Classic Check-in Labels grids), the Group editor with Configure Named Locations (Main + Overflow grids backed by GroupLocation rows, campus-context filtered picker) and the variant-driven Check-in Filters section that surfaces inherited-group-type attributes via AVC, the level-5 nesting cap on the inline Add Area action, and a GetTree block action that replaces the full-block reload on campus / Areas / Show-Inactive changes are in. The legacy .ascx / .ascx.cs are chopped, the page / route / block migration is written, and the conversion has been verified end-to-end against a real configuration. The worksurface panel-height overflow is accepted as-is and will not be pursued unless reported; the root cause and a candidate fix are recorded in Open Questions. | `61353768a59f44b7f7fe713a2e312f9a9531ac34`, `3e5b9d652c32a2cf22c81e36dfab1eb6d6070477` |
| [Create Connection Request SMS Action](communication/260506-sms-action-create-connection-request.md) | Communication | Josh Henninger | Add a new "Create Connection Request" SMS pipeline action that creates a Connection Request when an inbound SMS matches a configured keyword or filter, modeled directly on the existing "Launch Workflow" SMS action. Includes a separate estimate for converting the related SMS Pipeline Detail block from WebForms to Obsidian. | `35309ee571` |
| [PaymentInfo Carries Per-Account Detail to Gateways](finance/260603-paymentinfo-account-detail-for-gateways.md) | Finance | Nick Airdo | Surface per-Financial-Account detail (AccountId + Amount) to gateway components at the moment of charge so gateways can route transactions to the correct funds in their own systems. Achieved by adding an opt-in AccountAllocations list to PaymentInfo without changing any gateway interface signatures and without modifying any existing type. Purely additive to protect third-party gateway plugins. | `75c7895b999af069a4c2fb5ceaf98a73dbc24743` |
| [Group Schedule Toolbox Sign-Up Save Performance](group/260429-group-schedule-toolbox-signup-save-performance.md) | Group | Jon Edmiston | The Obsidian Group Schedule Toolbox's "Sign Up for Additional Times" Save action is slow because the server re-runs the full availability query for validation, performs N+1 attendance lookups, commits the attendance record twice, and re-queries the saved record to send the coordinator email. This spec proposes targeted fixes that preserve all current behavior (Save button, coordinator emails, validation rules) while making the action feel instant. | `b12da753220d1ffd819d827b05f3f2ae5844bcd3` |
| [GetPersonPhotoUrl SecuritySettings Bottleneck](core/260604-getpersonphotourl-securitysettings-bottleneck.md) | Core | Jason Hendee | Person.GetPersonPhotoUrl constructs a full SecuritySettingsService on every call just to read one boolean (DisablePredictableIds). The constructor hashes the entire security-settings JSON and performs cache work per call, which becomes a bottleneck when the method runs tens of thousands of times in a loop. | [`1e8caff2c5d8bfce84eabf2d74213e617f97d198`](https://github.com/SparkDevNetwork/Rock/commit/1e8caff2c5d8bfce84eabf2d74213e617f97d198) |
| [Smarty Streets International Location Service](core/260504-smarty-international-location-service.md) | Core | Nick Airdo | Rock v18 removed Bing-based address verification and geocoding, leaving international partners (e.g., Hope City Church, Edmonton, Canada) with no supported option for non-US address verification. This spec adds a standalone "Smarty Streets International" location service component that calls Smarty's International Street Address API for non-US addresses, leaves US addresses to the existing Smarty Streets component, and surfaces the same verification / geocoding configuration surface admins are used to. | [`29ecde8ef895c6d3b7aa8afcb47a6c3671d808b1`](https://github.com/SparkDevNetwork/Rock/commit/29ecde8ef895c6d3b7aa8afcb47a6c3671d808b1) |
| [Interaction Component Daily Count](core/260615-interaction-component-daily-count.md) | Core | Nick Airdo | Introduce an InteractionComponentDailyCount table that pre-aggregates daily interaction metrics per component, with an opt-in flag on InteractionChannel and a new step in the Rock Clean-up job that backfills missing dates from the last recorded date through yesterday using a SQL-based aggregation. | [`0a8f9d8813823eabfe929bf851c97f4df7c4dd0a`](https://github.com/SparkDevNetwork/Rock/commit/0a8f9d8813823eabfe929bf851c97f4df7c4dd0a) |
| [Hide the Registration Entry saved-account option when no usable login method exists](event/260615-registration-entry-passwordless-saved-account.md) | Event | Josh Henninger | On Passwordless-only sites, an anonymous registrar saving a payment method was forced to create an unusable Database login. The fix hides the "Save account information for future payments" option unless the registrar is logged in or Database authentication is enabled, and enforces the same rule server-side in the SaveFinancialAccountForm endpoint. Full Passwordless account entry was deferred as a separate feature. | [`f2774f6a6d`](https://github.com/SparkDevNetwork/Rock/commit/f2774f6a6d) |
| [Communication List Selection in the Simple Communication Entry Block](communication/260617-communication-entry-list-selection.md) | Communication | Joshua Henninger | Add Communication List selection to the simple Communication Entry block, gated behind a new "Enable Communication List Selection" block setting (default off) and limited to Full mode. Mirrors the Communication Wizard list picker, surfaces a per-medium reachable recipient count in the panel header, and persists Communication.ListGroupId so list membership resolves at send time. Segments are out of scope. | [`7332afe5e8`](https://github.com/SparkDevNetwork/Rock/commit/7332afe5e8) |
| [Rapid Attendance Entry Block Obsidian Conversion](check-in/260609-rapid-attendance-entry-obsidian-conversion.md) | Check-in | Jason Hendee | Convert the legacy WebForms Rapid Attendance Entry block (Check-in) to Obsidian, preserving all existing behavior and block settings, applying the approved Figma UX refresh, and adding a new Connection Opportunities section that creates connection requests on save. | [`ab9cc3951bbe8c4f457df2f4298298138c158344`](https://github.com/SparkDevNetwork/Rock/commit/ab9cc3951bbe8c4f457df2f4298298138c158344) |
| [LMS Assessment Retakes](lms/260623-lms-assessment-retakes.md) | LMS | Jason Hendee | Add configurable assessment retakes to Rock LMS. A per-activity point threshold gates whether a low-scoring submission is eligible for a retake; auto-graded activities assign retakes automatically, while manually graded activities let the facilitator decide. Assigning a retake deletes the prior completion and notifies the student. | [`7c0fac73aa5e1e7f09371e6f0036d71217a992c9`](https://github.com/SparkDevNetwork/Rock/commit/7c0fac73aa5e1e7f09371e6f0036d71217a992c9) |
| [Connection Request Entry](connection/260622-connection-request-entry.md) | Connection | Joshua Henninger | New public-facing Obsidian block (Connection domain) that lets a guest match or create their Person record, optionally add a spouse, supply contact and address details, and select one or more active Connection Opportunities to request involvement. Each selected opportunity creates a ConnectionRequest. Includes a new "Get Connected" external page and a fully configurable set of show/hide/require field settings and customizable text. | [`f5114d60ee`](https://github.com/SparkDevNetwork/Rock/commit/f5114d60ee) |
| [Connection Type List (Mobile) Backend](mobile/260608-mobile-connection-type-list.md) | Mobile | Panha Sim | Server-side mobile RockBlockType (ConnectionTypeListV2) that returns per-type Active-request summaries (five counts) for the authorized active connection types, with the count query duplicated from the web Connection Type Navigation block. First block in the Rock Mobile Connections V2 port. | `0c4790584b` |
| [Connection Type Detail / Opportunities (Mobile) Backend](mobile/260608-mobile-connection-type-detail.md) | Mobile | Panha Sim | Server-side mobile RockBlockType (ConnectionOpportunityListV2) hosting an Opportunities segment (per-opportunity five-count summaries, per-opportunity VIEW security) and a Details segment (campus-scoped Count-By-Status distribution plus 7-day Completion Metrics reused from ConnectionTypeService). Second block in the Connections V2 port. | `0c4790584b` |
| [Connection Opportunity Detail / Requests (Mobile) Backend](mobile/260609-mobile-connection-opportunity-detail.md) | Mobile | Panha Sim | Server-side mobile RockBlockType (ConnectionRequestListV2) returning a paged, server-filtered, server-sorted request list for one opportunity, with per-request celebration flag, due status, status pill, and per-request VIEW security honored (in-memory paging when EnableRequestSecurity is on). Third block in the Connections V2 port. | `0c4790584b` |
| [My Connection Requests (Mobile) Backend](mobile/260609-mobile-my-connection-requests.md) | Mobile | Panha Sim | Server-side mobile RockBlockType (MyConnectionRequests) returning, in one unpaged call, every request assigned to the current person as connector across all types and opportunities (per-request VIEW security honored), with each row carrying the display and client-side group/filter keys. Fourth block in the Connections V2 port. | `0c4790584b` |
| [Connection Request Detail (Mobile) Backend](mobile/260610-mobile-connection-request-detail.md) | Mobile | Panha Sim | Server-side mobile RockBlockType (ConnectionRequestDetailV2) that loads and edits one request (status, state, connector, campus, source, placement, comments, attributes), ships the activity feed plus add/edit/delete, transfer, workflow launch, celebration, and connection-request notes, with full request-level security. Fifth block in the Connections V2 port. | `0c4790584b` |
| [Add Connection Request V2 (Mobile) Backend](mobile/260616-mobile-add-connection-request.md) | Mobile | Panha Sim | Server-side mobile RockBlockType (AddConnectionRequestV2) backing the native multi-step wizard: cascading GetOptions (types, opportunities, statuses, connectors, placement groups, custom attributes), placement member attributes, and SaveConnectionRequest with placement and attribute persistence. Sixth block in the Connections V2 port. | `0c4790584b` |
| [Utility Payment Entry Obsidian Conversion](finance/260626-utility-payment-entry-obsidian-conversion.md) | Finance | Jason Hendee | Convert the Finance Utility Payment Entry WebForms block to Obsidian as part of the Fall 2025 / Spring 2026 block refresh. Applies the Figma UI polish, reorganizes block settings into tabs (with renamed labels and several new settings), relocates the CampusAccountAmountPicker account/campus mapping to the C# save path. | `0c925f34686f26b5ce498cdff5c1a44125a79dc9`, `0927480cffd1718ab39190e999e65a4099c1b8d8`, `ca82ed545685fed7cb30ea0af40bc138ae311765` |
| [Event Item Detail Polish + Personalization](event/260727-event-item-detail-polish-personalization.md) | Event | Jason Hendee | Polish the converted Calendar Event Item Detail (EventItemDetail) Obsidian edit block to the new section/stack design, and add the ability to assign personalization Audience Segments and Request Filters to an event item using the existing generic PersonalizedEntity mechanism. | [`322e2faeb7`](https://github.com/SparkDevNetwork/Rock/commit/322e2faeb7) |
| [Lava ToGuidV5 Filter](lava/260729-lava-toguidv5-filter.md) | Lava | Jon Edmiston | Adds an undocumented ToGuidV5 Lava filter and a supporting ToGuidV5 string extension method that derive a deterministic RFC 4122 version 5 Guid from a name and a namespace. The driving use case is hashing the organization Guid so it can be shared externally (specifically in the Rock Community Knowledge Base) without revealing the original value. Casing is normalized so the same Guid hashes identically regardless of how it was formatted, which is a deliberate deviation from canonical RFC 4122. | [`39266c38c9`](https://github.com/SparkDevNetwork/Rock/commit/39266c38c9) |
| [Scheduled Job Trigger Stuck in Blocked When RockJobListener Throws](core/260731-job-trigger-stuck-blocked-on-listener-throw.md) | Core | Jason Hendee | A scheduled job stops firing permanently (trigger stuck in Blocked) if RockJobListener.JobWasExecuted throws after the job runs, because the unguarded listener exception escapes into Quartz before the trigger lock is cleared. The failure is completely silent: the job shows "Success" and nothing is logged. Fix by guarding the listener bookkeeping steps so they log instead of throwing into the scheduler. | [`f4e72c1aa7`](https://github.com/SparkDevNetwork/Rock/commit/f4e72c1aa7) |
| [ServiceJobHistory: Sub-Second Jobs Produce Orphaned "Incomplete" Rows](core/260731-servicejobhistory-sub-second-orphaned-incomplete-rows.md) | Core | Jason Hendee | Sub-second job runs leave orphaned "Incomplete" ServiceJobHistory rows and mis-attributed run durations because the completion callback re-finds the "started" row by timestamp guessing. Fix by passing the row Id through Quartz's execution context and completing the row by primary key. | [`79760e3ac0`](https://github.com/SparkDevNetwork/Rock/commit/79760e3ac0) |
| [REST Body Validation Recurses Into Component and Cache Singletons](api/260803-rest-body-validation-component-recursion.md) | API | Jason Hendee | POST/PUT to seven entity models over the REST API takes 75 to 160 seconds because RockBodyModelValidator recursively walks the request graph into read-only properties that resolve live component and cache singletons, and from there into .NET reflection objects that hash entire assembly images. The work runs before authorization, so an unauthenticated caller can pin a worker thread at 100% CPU for over a minute per request. The fix replaces the validator's exact-name bypass list with category checks that skip server-resolved objects (Component subclasses, IEntityCache implementations) and reflection metadata, since none of those can carry request-body input. | [`495c3696e2`](https://github.com/SparkDevNetwork/Rock/commit/495c3696e24712010c9c38d19a55f7b3ec997106) |
| [Expose Entity GUIDs in Agent Tool Results](ai/260807-ai-agent-result-guids.md) | AI | Jon Edmiston | Agent tool results declared a Guid property, documented it as required for attribute work, and populated it nowhere; AttributeResult had no Guid at all. Adds Guid to AttributeResult along with the description, order, field type, and allowed values that AgentToolHelper already computed and discarded, and populates Guid in the eleven single-entity Get tools. Establishes the scope rule that Get results carry Guid and List/Lookup results do not, with a structural exception only where no partner Get exists. | [`839b6c3ca983851b471f31b8f67c8eb67f14f67d`](https://github.com/SparkDevNetwork/Rock/commit/839b6c3ca983851b471f31b8f67c8eb67f14f67d) |
| [DataSelectComponent Obsidian Grid Rendering](reporting/260811-dataselect-obsidian-grid-rendering.md) | Reporting | Daniel Hazelbaker | Add an Obsidian-side counterpart to DataSelectComponent.GetGridField so DataSelects can describe how their column renders in an Obsidian Grid, mirroring the WebForms/Obsidian split already in place for the admin configuration UI. Introduces a typed abstract ObsidianGridField hierarchy with seven root-tier subclasses (Text, Html, Boolean, Number, Currency, Date, DateTime) plus four Rock-shipped value-shaping subclasses (Label, Phone, List, Lava). Transforms receive a per-row ObsidianGridFieldContext POCO carrying the RockContext, the raw dynamic-typed row instance, per-column descriptors, an optional transformed-peer-values dictionary for late-binding fields, and a typed per-render cache accessor. Ships GetExportValue and SortFields machinery so future export improvements and click-sort correctness both land without an API break. Unblocks a future Obsidian ReportDetail block without breaking the WebForms path or existing plugin DataSelects. | `168da2b6fa` |
| [Connection Page Tree Cleanup (Phase 1 of Connection Page/Block Cleanup)](connection/260825-connection-page-tree-cleanup.md) | Connection | Jason Hendee | Phase 1 of the Connections page cleanup: collapse the duplicate Connections page tree into a single flat hierarchy under the Connections root and remove the Add Campaign Requests and Bulk Update Requests WebForms blocks. Page structure and block removal only; the Connections Hub add entry point and the link redirection sweep are deferred to later phases. | [`7d5c5bd4e9`](https://github.com/SparkDevNetwork/Rock/commit/7d5c5bd4e9) |
| [Lava Entity Command `.ToString()` Regression in `select:` Clauses](lava/260428-lava-entity-select-tostring-regression.md) | Lava | Jon Edmiston | After upgrading System.Linq.Dynamic.Core from 1.6.6 to 1.7.0, Lava entity commands that call .ToString() on a property whose name matches a CLR predefined type (Guid, DateTime, TimeSpan, etc.) inside a select: clause now fail to parse. Restore prior behavior by passing a ParsingConfig to the Dynamic.Core calls in RockEntityBlock. | [`c2cd8afb54`](https://github.com/SparkDevNetwork/Rock/commit/c2cd8afb54) |
| [Communication Wizard Attachment UI Consistency](communication/260506-communication-wizard-attachment-consistency.md) | Communication | Joshua Henninger | Replace the Communication Wizard's email-attachment UI (FileUploader card + bulleted file list) with the shared PillList framework control, matching the visual pattern used by the Simple Communication block so the two communication entry experiences look and behave consistently. Also migrate the Simple Communication block's recipient picker to PillList so the local popupPicker / attachmentPicker partials can be deleted entirely. | [`b933a3b0c5`](https://github.com/SparkDevNetwork/Rock/commit/b933a3b0c5) |
| [Persisted Dataset Cache Stale After Workflow / Job Refresh](cms/260513-persisted-dataset-cache-stale-after-workflow-refresh.md) | CMS | Jon Edmiston | Persisted Dataset refreshes performed by the workflow action, the scheduled job, the bus task, and the on-demand Lava filter leave PersistedDatasetCache and PersistedDatasetValueCache stale, so consumers (HTML blocks via the `\| PersistedDataset` Lava filter) keep showing old data even though the database row is current. Caused by a recent switch to `SaveChanges( true )`, which bypasses the cache-invalidation hook. | [`82b878cee9`](https://github.com/SparkDevNetwork/Rock/commit/82b878cee9) |
| [GeoPicker Drawing Library Replacement](core/260616-geopicker-drawing-library-replacement.md) | Core | Nick Airdo | Replace the deprecated google.maps.drawing.DrawingManager usage in the WebForms and Obsidian GeoPickers with an in-house drawing controller built on supported core overlays, so the picker keeps working past the v3.64 pin in the Google Maps JavaScript API and so the on-map toolbar can be tightened at the same time. | [`d30fced26b`](https://github.com/SparkDevNetwork/Rock/commit/d30fced26b) |
| [Grid Communicate URL Placeholder Encoding](core/260625-grid-communicate-url-placeholder-encoding.md) | Core | Carter Henning | The Obsidian grid's Communicate action builds its redirect URL with a {CommunicationId} placeholder. On sites whose communication page has no {CommunicationId} route, BuildUrl() puts the token in the query string and encodes the braces to %7b/%7d, so the client-side replace fails and the communication opens with no recipients. Switching the token to ((CommunicationId)), matching the existing ((Key)) navigation-URL convention, fixes it everywhere because parentheses are URL-safe. | [`b9f6614a62`](https://github.com/SparkDevNetwork/Rock/commit/b9f6614a62) |
| [Group Finder Obsidian Block](group/260708-group-finder-obsidian-block.md) | Group | Joshua Henninger | A new Obsidian Group Finder block modeled on Airbnb-style discovery: an opinionated pill filter bar (Campus, Where, When, What, More Filters), card results, and an optional interactive map. Reuses the existing GroupFinder filtering engine, adds a first-class Meeting Style group property, and ships alongside the current WebForms block (renamed "Group Finder (Legacy)"). | [`d1596f1d44`](https://github.com/SparkDevNetwork/Rock/commit/d1596f1d44) |
| [Sunset Protect My Ministry (v1) Background Check Component](core/260714-sunset-protect-my-ministry-v1.md) | Core | Nick Airdo | Retire the original Protect My Ministry (v1) background check component in Rock v20. Delete the PMM v1 code files, add a plugin data migration that removes the PMM EntityType, admin Page/Route/Block/BlockType, DVR Jurisdiction DefinedType, and component configuration attributes, and deactivate and rename the shared "Background Check" WorkflowType to "Background Check (PMM Legacy)" without losing historical Workflow rows. Keep historical PMM background-check documents viewable via the profile UI. | [`f75deadfd0`](https://github.com/SparkDevNetwork/Rock/commit/f75deadfd0) |
| [Core Administration Agent Skill](ai/260807-ai-agent-core-administration-skill.md) | AI | Jon Edmiston | A read-only AI agent skill exposing Rock's core configuration metadata: defined types and values, entity types, categories, field types, attribute definitions, and system communications. Thirteen tools, no writes. Nothing in it is workflow-specific, so any future page, block, report, or Lava authoring skill can build on the same surface. | [`b99c443925`](https://github.com/SparkDevNetwork/Rock/commit/b99c443925) |
| [Workflow Builder Agent Skill](ai/260807-ai-agent-workflow-builder-skill.md) | AI | Jon Edmiston | An AI agent skill that authors Rock workflow types. Fourteen tools that discover the installed workflow action components and their settings, then create, edit, and remove workflow types, attributes, activities, actions, and forms by writing entities directly. Container export and import are out of scope. Distinct from the existing WorkflowSkill, which executes a configured allow-list rather than authoring anything. | [`b99c443925`](https://github.com/SparkDevNetwork/Rock/commit/b99c443925) |
| [Google Map ID Styling Support for the Obsidian Map View](group/260807-mapview-google-map-id-styling.md) | Group | Joshua Henninger | Teach the shared Obsidian map view control to render a Rock Map Style through a Google cloud Map ID (native light/dark) when one is configured, falling back to the legacy Dynamic Map Style JSON path otherwise, and let the Map Style be the sole authority for POI/label visibility. | [`d1596f1d44`](https://github.com/SparkDevNetwork/Rock/commit/d1596f1d44) |
| [GetSystemPhoneNumber](ai/260811-ai-agent-get-system-phone-number.md) | AI | Jon Edmiston | Adds GetSystemPhoneNumber to the Communication skill, the partner Get for the existing LookupSystemPhoneNumbers. Returns one system phone number in full, including its unique identifier, which the lookup deliberately withholds. Without it there is no way to reach a system phone number's GUID through the agent, and no way to see the fourteen columns the lookup does not return. | [`b99c443925`](https://github.com/SparkDevNetwork/Rock/commit/b99c443925) |
| [Conditional Workflow Forms](ai/260811-ai-agent-workflow-conditional-forms.md) | AI | Jon Edmiston | Adds conditional visibility to workflow forms built through the Workflow Builder agent skill. A field can be shown only when another attribute holds a given value, which is the difference between a form that asks everything and a form that adapts. Extends the existing field input rather than adding a tool, because the rules belong to the field they govern. | [`b99c443925`](https://github.com/SparkDevNetwork/Rock/commit/b99c443925) |
| [Distributed Locking](core/260813-distributed-locking.md) | Core | Daniel Hazelbaker | Introduce a distributed locking primitive to Rock, built on the DistributedLock.SqlServer package, to prevent duplicate execution of Quartz jobs during app pool overlap and across farm nodes, and to provide a reusable coordination mechanism for other cross-node operations such as communication sending. | [`b398d8ef4b`](https://github.com/SparkDevNetwork/Rock/commit/b398d8ef4b) |
| [Community Knowledge Base Agent Skill](ai/260817-ai-agent-community-knowledge-base-skill.md) | AI | Jon Edmiston | An AI agent skill that reads the Rock community knowledge base over HTTP. Eight read-only tools across three remote stores: hybrid keyword and semantic search over documentation and community content, semantic and literal search over the Rock source, and progressive disclosure of curated topic trees. Category scope is the skill's only setting, and its picker populates from the remote managed lists so it never drifts from the server. | [`b99c443925`](https://github.com/SparkDevNetwork/Rock/commit/b99c443925) |
| [Page View Interaction Bot Filtering](core/260824-page-view-interaction-bot-filtering.md) | Core | Jon Edmiston | Page view interactions and Anonymous Visitor aliases are being written for bot traffic. Fixes the crawler classification ordering, moves the bot check ahead of Anonymous Visitor alias creation, replaces the hand-rolled crawler regex with a maintained pattern list, and adds prefetch/prerender guards. | [`69f42ed3b3`](https://github.com/SparkDevNetwork/Rock/commit/69f42ed3b3) |
| [Connections Hub Add Entry Point (Phase 2 of Connection Page/Block Cleanup)](connection/260825-connections-hub-add-entry-point.md) | Connection | Jason Hendee | Phase 2 of the Connections page cleanup: make the Connections Hub the entry point for creating a Connection Request. Adds a Request=0 add indicator, an IsAddConnectionRequestRequested option, and add modal preselection from the Connection Type or Opportunity. Hub block changes only; the link redirection sweep is Phase 3. | [`f9acac2205`](https://github.com/SparkDevNetwork/Rock/commit/f9acac2205) |
| [Connection Request Link Redirection (Phase 3 of Connection Page/Block Cleanup)](connection/260827-connection-request-link-redirection.md) | Connection | Jason Hendee | Phase 3 of the Connections page cleanup: repoint every core link that targets the Connection Request Detail page at the Connections Hub, renaming the query string parameters and switching the identifiers to IdKeys. Covers five code sites, two Lava templates, four LinkedPage defaults, and a migration that updates the shipped block attribute values. | [`9e9964ab4a`](https://github.com/SparkDevNetwork/Rock/commit/9e9964ab4a) |
| [Event Personalization Filtering for Calendar Surfaces](event/260826-event-item-personalization-consumers.md) | Event | Jon Edmiston | Event items can be tagged with personalization segments and request filters, but the public calendar surfaces ignore them. Adds opt-in filtering to the Calendar Lava block and the CalendarEvents Lava command. | [`c4db48721c`](https://github.com/SparkDevNetwork/Rock/commit/c4db48721c) |
